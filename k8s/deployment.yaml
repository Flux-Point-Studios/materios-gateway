apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: materios-gateway-data
  namespace: materios
spec:
  storageClassName: longhorn-1replica
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: materios-gateway
  namespace: materios
  labels:
    app: materios-gateway
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: materios-gateway
  template:
    metadata:
      labels:
        app: materios-gateway
    spec:
      imagePullSecrets:
        - name: ghcr-credentials
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/arch
                    operator: In
                    values:
                      - amd64
      initContainers:
        - name: fix-permissions
          image: busybox:latest
          command: ["sh", "-c", "chown -R 1000:1000 /data/blobs"]
          volumeMounts:
            - name: blob-data
              mountPath: /data/blobs
      containers:
        - name: materios-gateway
          image: ghcr.io/flux-point-studios/materios-gateway:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: materios-gateway-config
            - secretRef:
                name: materios-gateway-secret
          volumeMounts:
            - name: blob-data
              mountPath: /data/blobs
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: blob-data
          persistentVolumeClaim:
            claimName: materios-gateway-data
---
apiVersion: v1
kind: Service
metadata:
  name: materios-gateway
  namespace: materios
spec:
  type: NodePort
  selector:
    app: materios-gateway
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30300
